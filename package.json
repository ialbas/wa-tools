{
  "name": "wa-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Camada de privacidade e ocultação para WhatsApp Web — 100% local, sem contas, sem rede.",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "//deferred": "Stack de UI/build/E2E adicionada na fase de UI: wxt, react, react-dom, tailwindcss@4, @wppconnect/wa-js, playwright, @changemesh/e2e-kit (registry privado — precisa GITLAB_NPM_TOKEN)."
}
